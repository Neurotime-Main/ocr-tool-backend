services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_DB: ocr_highlighter
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
    ports:
      - "5432:5432"
    volumes:
      - ocr_postgres_data:/var/lib/postgresql/data

volumes:
  ocr_postgres_data:
